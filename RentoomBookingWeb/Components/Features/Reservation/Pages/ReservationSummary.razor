@page "/nowarezerwacja/{id:int}";

@*
    TODO: 
    here put a nice form to summarize the reservation details before confirming the booking.
    Add "Zarezerwuj z poleceniem zaplaty" button at the bottom.
    Add "Cofnij" button to go back to the previous page.
    Cofnij button should navigate back to /rezerwacja/{idApartmentId}
    Cofnij button should remove any temporary reservation data if exists in idobooking service.(delete the unconfirmed reservation))
    when the button is clicked, navigate to /platnosc/{idReservationId}

*@ 

@code {
//
}
