@inherits LayoutComponentBase
@using RentoomBooking.SharedClasses.Models;
@using RentoomBooking.StayWell.States
@inject ReservationState ReservationState
@inject MediaState MediaState


<div class="layout-page">
    <div class="layout-header">
        <RentoomBooking.StayWell.Components.Header Token="@ReservationState.CurrentToken" reservationId="@ReservationState.CurrentReservation?.Reservation.id.ToString()">
        </RentoomBooking.StayWell.Components.Header>
    </div>

    <div class="layout-body">
        @Body
    </div>

    <div class="layout-footer-nav">
        <RentoomBooking.StayWell.Components.FooterNavigation Token="@ReservationState.CurrentToken">
        </RentoomBooking.StayWell.Components.FooterNavigation>
    </div>
</div>


@code {

    protected override void OnInitialized()
    {
        ReservationState.OnChange += StateHasChanged;
        InvokeAsync(StateHasChanged);
    }

    public void Dispose()
    {
        ReservationState.OnChange -= StateHasChanged;
    }
}